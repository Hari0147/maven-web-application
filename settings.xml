<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0
                              https://maven.apache.org/xsd/settings-1.0.0.xsd">

    <!-- ===================== -->
    <!-- Local Repository Path -->
    <!-- ===================== -->
    <localRepository>/home/user/.m2/repository</localRepository>

    <!-- ========= -->
    <!-- Proxies   -->
    <!-- ========= -->
    <proxies>
        <proxy>
            <id>corp-proxy</id>
            <active>true</active>
            <protocol>http</protocol>
            <host>proxy.company.com</host>
            <port>8080</port>
            <nonProxyHosts>localhost|127.0.0.1</nonProxyHosts>
        </proxy>
    </proxies>

    <!-- ========= -->
    <!-- Servers   -->
    <!-- Used for authentication (Nexus, GitHub Packages, etc.) -->
    <!-- ========= -->
    <servers>
        <server>
            <id>webapp-release</id>
            <username>admin</username>
            <password>nexus</password>
        </server>
 
	<server>
            <id>webapp-snapshot</id>
 	    <username>admin</username>
	    <password>nexus</password>
	</server>

    </servers>

    <!-- ========= -->
    <!-- Mirrors   -->
    <!-- Redirect all Maven Central traffic to Nexus -->
    <!-- =========
    <mirrors>
        <mirror>
            <id>nexus</id>
            <mirrorOf>*</mirrorOf>
            <url>http://nexus.company.com:8081/repository/maven-public/</url>
        </mirror>
    </mirrors>
    -->

    <!-- ========= -->
    <!-- Profiles  -->
    <!-- ========= -->
    <profiles>
        <profile>
            <id>nexus</id>

            <repositories>
                <repository>
                    <id>central</id>
                    <url>http://nexus.company.com:8081/repository/maven-central/</url>
                    <releases>
                        <enabled>true</enabled>
                    </releases>
                    <snapshots>
                        <enabled>false</enabled>
                    </snapshots>
                </repository>

                <repository>
                    <id>snapshots</id>
                    <url>http://nexus.company.com:8081/repository/maven-snapshots/</url>
                    <releases>
                        <enabled>false</enabled>
                    </releases>
                    <snapshots>
                        <enabled>true</enabled>
                    </snapshots>
                </repository>
            </repositories>

            <pluginRepositories>
                <pluginRepository>
                    <id>central</id>
                    <url>http://nexus.company.com:8081/repository/maven-central/</url>
                </pluginRepository>
            </pluginRepositories>
        </profile>
    </profiles>

    <!-- ================= -->
    <!-- Active Profiles   -->
    <!-- ================= -->
    <activeProfiles>
        <activeProfile>nexus</activeProfile>
    </activeProfiles>

</settings>

